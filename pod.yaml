apiVersion: v1
kind: pod
metadata:
  name: mypod
  labels:
    app: myapp
spec:
  containers:
  - name: nginx-container
    image: nginx:latest
    ports:
      name: nginx-port
      containerPort: 80